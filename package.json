{
  "name": "world-monitor",
  "version": "1.0.0",
  "description": "World Monitor - Global Geopolitical Dashboard",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
